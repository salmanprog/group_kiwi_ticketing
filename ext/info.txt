RewriteEngine on
RewriteCond %{HTTP_HOST} ^group.kiwiticketing.com$ [NC,OR]
RewriteCond %{HTTP_HOST} ^www.group.kiwiticketing.com$
RewriteCond %{REQUEST_URI} !public/
RewriteRule (.*) /public/$1 [L]


==============================htaccess end------------------------------------
ALTER TABLE `contract_items` ADD `product_id` INT NOT NULL AFTER `id`;
ALTER TABLE `organizations` CHANGE `event_history_id` `event_history_id` BIGINT(20) UNSIGNED NULL DEFAULT '0';

ALTER TABLE `user_order_tickets` ADD `status` ENUM('1','0') NULL DEFAULT '0' AFTER `id`;

ALTER TABLE `contracts` ADD `ticket_enable` ENUM('0','1') NOT NULL DEFAULT '0' AFTER `id`;


CREATE TABLE company_account_config (
    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    
    auth_code VARCHAR(255) NOT NULL,
    company_id INT(255) NOT NULL,
    
    stripe_key_status ENUM('test', 'live') DEFAULT 'test',
    
    test_publishable_key VARCHAR(255) NULL,
    test_secret_key VARCHAR(255) NULL,
    
    live_publishable_key VARCHAR(255) NULL,
    live_secret_key VARCHAR(255) NULL,
    
    created_at TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);


ALTER TABLE `contracts` ADD `isAllowedForPrinting` ENUM('0','1') NOT NULL DEFAULT '0' AFTER `id`;


CREATE TABLE `company_media` (
    `id` BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    
    `auth_code` VARCHAR(255) NULL,
    `filename` VARCHAR(255) NULL,
    `original_name` VARCHAR(255) NULL,
    `file_url` TEXT NULL,
    `mime_type` VARCHAR(255) NULL,
    `file_type` VARCHAR(100) NULL,
    `driver` VARCHAR(100) NULL,
    `media_type` VARCHAR(100) NULL,
    
    `meta` JSON NULL,

    `created_at` TIMESTAMP NULL DEFAULT NULL,
    `updated_at` TIMESTAMP NULL DEFAULT NULL,
    `deleted_at` TIMESTAMP NULL DEFAULT NULL
);

ALTER TABLE `company_media` ADD `slug` VARCHAR(200) NOT NULL AFTER `id`;


ALTER TABLE `company` ADD `logo_url` TEXT NULL AFTER `login_url`;

ALTER TABLE `user_estimate_item_taxes` ADD `amount` INT(11) NOT NULL DEFAULT '0' AFTER `percentage`

kiwiticketing
u2-nqewjfywa1fd@c1102859.sgvps.net
18765

dev@ideaseat.com
7Q1cbxQ(G\04

